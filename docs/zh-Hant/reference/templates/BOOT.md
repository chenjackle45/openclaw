---
title: "BOOT(啟動模板)"
summary: "BOOT.md 的 Workspace 模板"
read_when:
  - 新增 BOOT.md 檢查清單
---
# BOOT.md

新增簡短、明確的指令說明 OpenClaw 在啟動時應該做什麼（啟用 `hooks.internal.enabled`）。
如果任務傳送訊息，使用 Message Tool 然後以 NO_REPLY 回覆。
